import SQLEditor from '@/components/editor'; // Assuming SQLEditor is in the same directory
// Then use it in your JSX
export default function QueryEditor() {
  return (
    <div>
      <SQLEditor />
    </div>
  );
}